# Windows Console Toolbox

These are some very useful console scripts that are not specific to my environment.

### bin

  * findcmd.cmd - Search macros, DOS and path for a command

  * listpath.cmd - List env PATH in separate paths as a list along with INCLUDE and LIB environment variables.
